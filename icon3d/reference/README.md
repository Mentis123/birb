# Reference evidence — never imported by the page

- `copilot-2023-wikipedia-render.png` — Wikipedia's 500px render of the official
  2023 vector (the same path data transcribed in `src/icons/copilot-2023.js`).
  The user's brief screenshot is this artwork.
- `copilot-2026-commons-render.png` — Commons' render of the 2026 vector.
- `plates-build-desktop.png` — the flat extruded-plates build (Plates mode),
  the "Blender import" look the study started from.

The brief also showed a Blender viewport: the same icon imported as SVG curves,
extruded into flat plates standing on a rounded square tile, the pink band
(painted last) in front and the blue band behind. That screenshot is not in the
repository; the plates build above reproduces it.
